# План: добавить healthcheck-эндпоинт

## Цель
Добавить `GET /health`, возвращающий `{"status":"ok"}` и код 200.

## Шаги
1. Зарегистрировать роут `GET /health` в существующем роутере.
2. Вернуть статичный JSON `{"status":"ok"}` со статусом 200.
3. Добавить один тест: запрос к `/health` → 200 и тело `{"status":"ok"}`.

## Acceptance
- Запрос `GET /health` отвечает 200 и телом `{"status":"ok"}`.
- Тест проходит.

<!--
Намеренно оставлены ТОЛЬКО nit-замечания для проверки stop_policy:
- непоследовательная нумерация заголовков в более крупных версиях плана;
- стилистика оформления списков.
Никаких blocker/major/minor по существу: цель, шаги, критерии приёмки и сверка с кодом
тривиальны и непротиворечивы.
-->
